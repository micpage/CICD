--liquibase formatted sql

--changeset your.name:3
CREATE TABLE TA_BOOKS(
  BOOK_ID        INT GENERATED BY DEFAULT AS IDENTITY NOT NULL, 
  TITLE          VARCHAR(255) NOT NULL, 
  TOTAL_PAGES    INT NULL, 
  RATING         DECIMAL(4, 2) NULL, 
  ISBN           VARCHAR(13) NULL, 
  PUBLISHED_DATE DATE, 
  PUBLISHER_ID   INT NULL
);

CREATE UNIQUE INDEX XPKTA_BOOKS ON TA_BOOKS (BOOK_ID);

ALTER TABLE TA_BOOKS ADD CONSTRAINT XPKTA_BOOKS PRIMARY KEY (BOOK_ID);

CALL SYSPROC.ADMIN_CMD ( 'RUNSTATS ON TABLE TA_BOOKS ON ALL COLUMNS SET PROFILE' );
