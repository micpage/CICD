--liquibase formatted sql

--changeset book.master:1
CREATE TABLE TA_PUBLISHERS(
  PUBLISHER_ID INT GENERATED BY DEFAULT AS IDENTITY NOT NULL, 
  NAME         VARCHAR(255) NOT NULL
);

CREATE UNIQUE INDEX XPKTA_PUBLISHERS ON TA_PUBLISHERS (PUBLISHER_ID);

ALTER TABLE TA_PUBLISHERS ADD CONSTRAINT XPKTA_PUBLISHERS PRIMARY KEY (PUBLISHER_ID);

CALL SYSPROC.ADMIN_CMD ( 'RUNSTATS ON TABLE TA_PUBLISHERS ON ALL COLUMNS SET PROFILE' );
